apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: del
spec:
  rules:
    - host: example.cpm
      http:
         paths: 
           - path: /
             pathType: prefix
             backend:
               service:
                    name
               port:
                 number: 

           - path: /api
             pathType: prefix
             backend:
               service: 
                  name:
               port:
                  number:
